6. Dependencies
Make sure you have zip installed:

sudo apt install zip   # On Ubuntu/Debian
sudo dnf install zip   # On Fedora

7. Troubleshooting
If you get a "permission denied" error, make sure the script is executable (chmod +x backup_script.sh).
If you see "zip command not found," install zip as above.
